<Window x:Class="AdRev.Desktop.Windows.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Saisie" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource CalmBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="InstructionText" Text="Entrez l'information :" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{StaticResource CalmHeaderBrush}"/>
        <TextBox x:Name="InputTextBox" Grid.Row="1" materialDesign:HintAssist.Hint="URL ou Titre..." Style="{StaticResource MaterialDesignFloatingHintTextBox}" VerticalAlignment="Center"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Annuler" IsCancel="True" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"/>
            <Button Content="Valider" Click="Ok_Click" IsDefault="True" Style="{StaticResource MaterialDesignRaisedButton}" Background="{StaticResource PrimaryBrush}"/>
        </StackPanel>
    </Grid>
</Window>


