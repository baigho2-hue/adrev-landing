<Window x:Class="AdRev.Desktop.VariableDesignerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdRev.Desktop"
        mc:Ignorable="d"
        Title="AdRev - Concepteur de Variables" Height="Auto" Width="Auto"
        MinHeight="600" MinWidth="1024"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/AdRev.Desktop;component/Assets/AppIcon.png"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <local:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>
        
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="ExtraBold"/>
            <Setter Property="Foreground" Value="{StaticResource CalmSubHeaderBrush}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>

        <Style x:Key="CalmCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource CalmBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Main Content -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- 1. Header Area -->
        <Border Grid.Row="0" Background="{StaticResource CalmHeaderBrush}" Padding="25,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal">
                    <Image Source="pack://application:,,,/AdRev.Desktop;component/Assets/AppIcon.png" Width="30" Height="30" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="CONCEPTEUR DE VARIABLES SCIENTIFIQUES" Foreground="White" FontSize="22" FontWeight="Black"/>
                        <TextBlock Text="Structurez vos données de collecte avec précision et rigueur académique." Foreground="#94A3B8" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <Border Grid.Column="1" Background="#1E293B" CornerRadius="8" Padding="15,8" VerticalAlignment="Center" BorderBrush="#334155" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="PROJET : " FontWeight="SemiBold" Foreground="#64748B" FontSize="11" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtProjectTitle" Text="Nouveau Projet" FontWeight="Bold" Foreground="#F8FAFC" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- 2. Main Work Area -->
        <Grid Grid.Row="1" Background="{StaticResource CalmBackgroundBrush}">
            <Grid.Margin>
                <Thickness Left="20" Top="20" Right="20" Bottom="20"/>
            </Grid.Margin>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/> <!-- List Sidebar -->
                <ColumnDefinition Width="420"/> <!-- Editor -->
                <ColumnDefinition Width="*"/>   <!-- Preview -->
            </Grid.ColumnDefinitions>

            <!-- LEFT: Variable List -->
            <Border Grid.Column="0" Style="{StaticResource CalmCardStyle}" Background="{StaticResource CalmAltBackgroundBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="DICTIONNAIRE" Style="{StaticResource SectionHeaderStyle}" Margin="15,15,15,10"/>
                    
                    <ListBox x:Name="VariablesListBox" Grid.Row="1" BorderThickness="0" Background="Transparent" SelectionChanged="VariablesListBox_SelectionChanged" HorizontalContentAlignment="Stretch" Padding="5">
                        <ListBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <Border Background="#E2E8F0" Padding="10,5" CornerRadius="4" Margin="0,5">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="{StaticResource CalmSubHeaderBrush}" FontSize="12"/>
                                        </Border>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ListBox.GroupStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10" Margin="0,2" CornerRadius="6" x:Name="ItemBorder">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="30"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="24" Height="24" CornerRadius="12" Background="{StaticResource CalmBackgroundBrush}" HorizontalAlignment="Left">
                                            <TextBlock Text="V" Foreground="{StaticResource CalmSubHeaderBrush}" FontWeight="Bold" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Prompt}" FontWeight="SemiBold" FontSize="12" TextTrimming="CharacterEllipsis" Foreground="{StaticResource CalmTextBrush}"/>
                                            <TextBlock Text="{Binding Name}" FontSize="10" Foreground="#64748B"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Grid Grid.Row="2" Margin="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="BtnAddVariable" Click="BtnAddVariable_Click" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource SecondaryBrush}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0"/>
                                <TextBlock Text="Ajouter une Variable"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BtnDeleteVariable" Grid.Column="1" Click="BtnDeleteVariable_Click" Margin="10,0,0,0" Width="40" Background="#FFEBEE" Foreground="#D32F2F" BorderBrush="#FFCDD2" ToolTip="Supprimer">
                            <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets"/>
                        </Button>
                    </Grid>
                </Grid>
            </Border>

            <!-- CENTER: Editor -->
            <ScrollViewer Grid.Column="1" Margin="0" VerticalScrollBarVisibility="Auto">
                <Border Style="{StaticResource CalmCardStyle}" Background="White">
                    <StackPanel x:Name="EditorPanel" IsEnabled="False">
                        <TextBlock Text="CONFIGURATEUR" Style="{StaticResource SectionHeaderStyle}"/>
                        
                        <Expander Style="{StaticResource ScientificExpanderStyle}" Header="Informations Générales" Tag="#F1F5F9" Foreground="{StaticResource CalmSubHeaderBrush}" IsExpanded="True" Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Text="Question (Libellé)" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="TxtPrompt" Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TextChanged="TxtPrompt_TextChanged" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                                
                                <Grid Margin="0,15,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Nom Technique" Style="{StaticResource LabelStyle}"/>
                                        <TextBox x:Name="TxtName" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" CharacterCasing="Upper"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Type de Donnée" Style="{StaticResource LabelStyle}"/>
                                        <ComboBox x:Name="CmbType" SelectionChanged="CmbType_SelectionChanged">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>
                                
                                <TextBlock Text="Groupe / Section" Style="{StaticResource LabelStyle}" Margin="0,15,0,5"/>
                                <TextBox x:Name="TxtGroup" TextChanged="TxtGroup_TextChanged"/>
                                
                                <CheckBox x:Name="ChkRequired" Content="Variable Obligatoire (*)" Margin="0,15,0,0" FontWeight="Bold" Click="ChkRequired_Click" Foreground="#D32F2F"/>
                            </StackPanel>
                        </Expander>

                        <Expander Style="{StaticResource ScientificExpanderStyle}" Header="Logique et Visibilité" Tag="#F8FAFC" Foreground="{StaticResource CalmSubHeaderBrush}" Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Text="Condition d'Affichage" Style="{StaticResource LabelStyle}"/>
                                <TextBox x:Name="TxtCondition" TextChanged="TxtCondition_TextChanged" ToolTip="Ex: [SEXE] = 'F'"/>
                                <TextBlock Text="Laissez vide pour une visibilité permanente." FontSize="10" Foreground="Gray" FontStyle="Italic"/>

                                <TextBlock Text="Logique de Saut (Skip)" Style="{StaticResource LabelStyle}" Margin="0,15,0,5"/>
                                <TextBox x:Name="TxtSkipLogic" TextChanged="TxtSkipLogic_TextChanged" ToolTip="Ex: Aller à la section B"/>
                            </StackPanel>
                        </Expander>

                        <!-- Paramètres Spécifiques (Dynamic) -->
                        <Border x:Name="OptionsPanel" Background="#F8F9FA" CornerRadius="8" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="PARAMÈTRES AVANCÉS" FontWeight="Bold" FontSize="11" Foreground="{StaticResource CalmSubHeaderBrush}" Margin="0,0,0,10"/>
                                
                                <StackPanel x:Name="PanelChoices" Visibility="Collapsed">
                                    <TextBlock Text="Options de réponse (Séparez par des virgules)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox x:Name="TxtChoices" Height="80" TextWrapping="Wrap" AcceptsReturn="True" TextChanged="TxtChoices_TextChanged"/>
                                </StackPanel>

                                <StackPanel x:Name="PanelNumeric" Visibility="Collapsed">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="Min" Style="{StaticResource LabelStyle}"/>
                                            <TextBox x:Name="TxtMin" TextChanged="TxtMin_TextChanged"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Max" Style="{StaticResource LabelStyle}"/>
                                            <TextBox x:Name="TxtMax" TextChanged="TxtMax_TextChanged"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>

                                <StackPanel x:Name="PanelCalculated" Visibility="Collapsed">
                                    <TextBlock Text="Formule Mathématique" Style="{StaticResource LabelStyle}"/>
                                    <TextBox x:Name="TxtFormula" Height="60" TextWrapping="Wrap" AcceptsReturn="True" TextChanged="TxtFormula_TextChanged" Background="#E8EAF6"/>
                                </StackPanel>
                                
                                <StackPanel x:Name="PanelQualitativeDeductive" Visibility="Collapsed">
                                    <TextBlock Text="Classification Scientifique" Style="{StaticResource LabelStyle}"/>
                                    <TextBox x:Name="TxtTheme" TextChanged="TxtTheme_TextChanged" Tag="Thème"/>
                                    <TextBox x:Name="TxtSubTheme" TextChanged="TxtSubTheme_TextChanged" Tag="Sous-thème" Margin="0,8,0,0"/>
                                </StackPanel>

                                <!-- Recoding Section -->
                                <StackPanel Margin="0,15,0,0">
                                    <TextBlock Text="Transformation" Style="{StaticResource LabelStyle}"/>
                                    <Button x:Name="BtnRecode" Content="Définir les règles de recodage..." Click="BtnRecode_Click"
                                            Background="#E0F7FA" Foreground="#006064" BorderBrush="#B2EBF2"/>
                                    <TextBlock Text="Définit comment cette variable sera transformée lors de l'analyse." 
                                               FontSize="10" Foreground="Gray" FontStyle="Italic" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <!-- RIGHT: Live Preview with Times New Roman -->
            <Grid Grid.Column="2" Margin="10,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Border Background="{StaticResource CalmSubHeaderBrush}" Padding="15,10" CornerRadius="8,8,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE890;" FontFamily="Segoe MDL2 Assets" Foreground="#F1F5F9" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16"/>
                        <TextBlock Text="APERÇU SCIENTIFIQUE (Le rendu final)" Foreground="#F8FAFC" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource CalmSubHeaderBrush}" BorderThickness="1,0,1,1" CornerRadius="0,0,8,8">
                    <TextBox x:Name="TxtPreview" 
                             BorderThickness="0"
                             FontFamily="Times New Roman" 
                             FontSize="14" 
                             Padding="30" 
                             AcceptsReturn="True" 
                             VerticalScrollBarVisibility="Auto" 
                             IsReadOnly="True" 
                             TextWrapping="Wrap"
                             Background="White"
                             Foreground="#333"/>
                </Border>
            </Grid>
        </Grid>

        <!-- 3. Footer Operations -->
        <Border Grid.Row="2" Background="#F8FAFC" Padding="25,18" BorderBrush="{StaticResource CalmBorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Click="BtnExport_Click" Style="{StaticResource ModernButtonStyle}" Background="#EA580C" Foreground="White">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8A1;" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Exporter en Word (MS Word)" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Fermer" Click="Close_Click" Margin="0,0,15,0" Width="120" Background="Transparent" Foreground="#475569" BorderBrush="#94A3B8"/>
                    <Button Content="ENREGISTRER LE MASQUE" Click="BtnSave_Click" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource CalmSubHeaderBrush}" Foreground="White" Width="280"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
