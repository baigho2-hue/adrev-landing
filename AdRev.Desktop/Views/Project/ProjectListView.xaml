<UserControl x:Class="AdRev.Desktop.Views.Project.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000" Background="Transparent">
             
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header Section 1 -->
            <RowDefinition Height="Auto"/> <!-- Recent Projects (Horizontal) -->
            <RowDefinition Height="Auto"/> <!-- Header Section 2 -->
            <RowDefinition Height="*"/>    <!-- All Projects (List) -->
        </Grid.RowDefinitions>

        <!-- Section 1 : Projets Récents -->
        <TextBlock Text="Projets Récents" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Foreground="#6366F1" Margin="0,0,0,15"/>
        
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,30">
            <ItemsControl x:Name="RecentProjectsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Width="220" Height="140" Margin="0,0,15,5" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <Border Background="#EEF2FF" CornerRadius="8,8,0,0" Padding="10" Height="40">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FileDocument" Foreground="#6366F1" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding LastModified}" FontSize="10" Foreground="#6366F1" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                
                                <TextBlock Grid.Row="1" Text="{Binding Name}" FontWeight="Bold" TextWrapping="Wrap" Margin="10,10,10,0" Foreground="#1E293B"/>
                                <TextBlock Grid.Row="1" Text="{Binding Type}" FontSize="11" Margin="10,45,-10,0" Foreground="Gray"/>
                                
                                <Button Grid.Row="2" Style="{StaticResource MaterialDesignFlatButton}" Content="Ouvrir" Height="30" FontSize="11" HorizontalAlignment="Right" Foreground="#6366F1" Margin="5"/>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Section 2 : Tous les Projets -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Tous les anciens projets" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Foreground="#64748B"/>
            <TextBox Grid.Column="1" Width="250" materialDesign:HintAssist.Hint="Rechercher un projet..." Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        </Grid>

        <materialDesign:Card Grid.Row="3" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8">
            <DataGrid x:Name="AllProjectsGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nom du Projet" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="Type d'Étude" Binding="{Binding Type}" Width="*"/>
                    <DataGridTextColumn Header="Statut" Binding="{Binding Status}" Width="*"/>
                    <DataGridTextColumn Header="Dernière Modification" Binding="{Binding LastModified}" Width="*"/>
                    <DataGridTemplateColumn Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Ouvrir" Style="{StaticResource MaterialDesignFlatButton}" Click="OpenProject_Click" Foreground="#6366F1"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
